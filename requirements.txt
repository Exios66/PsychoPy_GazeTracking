# Core dependencies
psychopy==2024.2.5
numpy>=1.22.0,<1.25.0
pandas>=1.5.0,<2.1.0
scipy>=1.9.0
matplotlib>=3.6.0
pillow>=9.0.0
opencv-python>=4.6.0
websockets>=10.0.0
pyglet>=1.5.0,<2.0.0
pyyaml>=6.0
pyopengl>=3.1.0
pyzmq>=23.0.0
imageio>=2.19.0
imageio-ffmpeg>=0.4.7
moviepy>=1.0.3

# Web application dependencies
aiohttp>=3.8.0
python-dotenv>=1.0.0
sqlalchemy>=2.0.0
flask>=2.2.0
flask-cors>=3.0.10

# Development tools
pytest>=7.0.0
black>=23.0.0
mypy>=1.0.0

# Note: The following packages may require system dependencies
# and should be installed manually if needed:
# - wxPython (GUI toolkit)
# - psychtoolbox (timing)
# - tables (HDF5 support)
# - ffpyplayer (video playback)
# - soundfile (audio processing)
# - psycopg2-binary (PostgreSQL)
# - tobii-research (eye tracking)
# - pylink (eye tracking) 